services:
  mysql:
    image: mysql
    ports:
      - "3306:3306" 
    expose:
      - "3306"
    environment:
      - MYSQL_DATABASE=sistema_produtos
      - MYSQL_USER=
      - MYSQL_PASSWORD=
      - MYSQL_ROOT_PASSWORD=
    volumes:
      - dados_mysql:/var/lib/mysql

volumes:
  dados_mysql: